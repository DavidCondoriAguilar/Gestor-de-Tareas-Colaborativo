# Variables de entorno para ASP.NET Core (ejemplo)
# Copia este archivo a `.env` y coloca tus valores reales. Nunca subas `.env`.

# Connection string (sobrescribe appsettings.json y User Secrets si está activa)
# OJO: Usa solo en local o CI controlado
ConnectionStrings__DefaultConnection="Host=localhost;Database=gestor_tareas_db;Username=postgres;Password=YOUR_PASSWORD"
